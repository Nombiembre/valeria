---
const { class: className, text, ...props } = Astro.props;

---

<button class={`bg-primary text-black px-4 py-2 rounded-full block mx-auto transition-all hover:bg-transparent border border-transparent hover:border-primary hover:text-primary cursor-pointer ${className}`} {...props}>
  {text}
</button>
